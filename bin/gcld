#!/usr/bin/env bash
#
# Git clone with depth 1 and branch.

url=$1
branch=$2
directory=$3
if [[ $# -eq 1 ]]; then
  git clone --depth 1 "$url"
elif [[ $# -eq 2 ]]; then
  git clone --depth 1 -b "$branch" "$url"
elif [[ $# -eq 3 ]]; then
  git clone --depth 1 -b "$branch" "$url" "$directory"
fi
