#!/bin/sh
#
# Example:
#   openssl_expiration example.com

if [ $# != 1 ]; then
  echo 'Usage: openssl_expiration example.com'
  echo 'e.g. openssl_expiration example.com'
  exit 1
fi
echo | openssl s_client -connect $1:443 -servername $1 2>/dev/null | openssl x509 -noout -dates
