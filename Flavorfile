# TODO Add YouCompleteMe

# == Languages

# Go development plugin for Vim
flavor 'fatih/vim-go'

# Syntax Highlight for Vue.js components
flavor 'posva/vim-vue', branch: 'master'

# groovy indent script 
flavor 'vim-scripts/groovyindent-unix'

# highlights logstash configuration files
flavor 'robbles/logstash.vim', branch: 'master'

# distinct highlighting of keywords vs values, JSON-specific (non-JS) warnings, quote concealing. Pathogen-friendly.
flavor 'elzr/vim-json', branch: 'master'

# == Git

# A Git wrapper so awesome, it should be illegal 
flavor 'tpope/vim-fugitive'

# A Vim plugin which shows a git diff in the gutter (sign column) and stages/undoes hunks.
flavor 'airblade/vim-gitgutter', branch: 'master'

# == Ruby

# Vim/Ruby Configuration Files
flavor 'vim-ruby/vim-ruby', branch: 'master'

# Lightweight support for Ruby's Bundle
flavor 'tpope/vim-bundler', branch: 'master'

# Ruby on Rails power tools
flavor 'UzxMx/vim-rails', branch: 'master'

# it's like rails.vim without the rails
flavor 'tpope/vim-rake', branch: 'master'

# slim syntax highlighting for vim.
flavor 'slim-template/vim-slim', branch: 'master'

# Run Rspec specs from Vim 
flavor 'thoughtbot/vim-rspec', branch: 'master'

# == Build

# Asynchronous build and test dispatcher
flavor 'tpope/vim-dispatch', branch: 'master'

# == File and Directory

# A tree explorer plugin for vim.
flavor 'scrooloose/nerdtree', branch: 'master'

# This plugin add capability to search in folders via NERDtree.
flavor 'tyok/nerdtree-ack', branch: 'master'

# Automatically create any non-existent directories before writing the buffer.
flavor 'pbrisbin/vim-mkdir', branch: 'master'

# == Filetype

# Context filetype library for Vim script
flavor 'Shougo/context_filetype.vim', branch: 'master'

# Vim comment plugin: supported operator/non-operator mappings, repeatable by dot-command
flavor 'tyru/caw.vim'

# A Filetype plugin for csv files
flavor 'chrisbra/csv.vim', branch: 'master'

# == Lint/Format

# Asynchronous Lint Engine
flavor 'w0rp/ale', branch: 'master'

# Provide easy code formatting in Vim by integrating existing code formatters.
flavor 'chiel92/vim-autoformat', branch: 'master'

# Align text
flavor 'godlygeek/tabular'

# A simple, easy-to-use Vim alignment plugin.
flavor 'junegunn/vim-easy-align'

# A vim plugin to display the indention levels with thin vertical lines
flavor 'Yggdroot/indentLine', branch: 'master'

# == Autocomplete

# Insert or delete brackets, parens, quotes in pair.
flavor 'jiangmiao/auto-pairs', branch: 'master'

# wisely add "end" in ruby, endfunction/endif/more in vim script, etc
flavor 'tpope/vim-endwise', branch: 'master'

# Auto close (X)HTML tags
flavor 'alvan/vim-closetag', branch: 'master'

# Perform all your vim insert mode completions with Tab
flavor 'ervandew/supertab', branch: 'master'

# Yet Another Remote Plugin Framework for Neovim
flavor 'roxma/nvim-yarp', branch: 'master'

flavor 'roxma/vim-hug-neovim-rpc', branch: 'master'

# Dark powered asynchronous completion framework for neovim/Vim8
# Depends on roxma/nvim-yarp and roxma/vim-hug-neovim-rpc
flavor 'Shougo/deoplete.nvim', branch: 'master'

# == Find

# Full path fuzzy file, buffer, mru, tag finder for Vim.
flavor 'ctrlpvim/ctrlp.vim', branch: 'master'

# An ack.vim alternative mimics Ctrl-Shift-F on Sublime Text 2
flavor 'dyng/ctrlsf.vim', branch: 'master'

# Vim plugin for the Perl module / CLI script 'ack'
# Need to install ack or ag.
flavor 'mileszs/ack.vim', branch: 'master'

# Show status when searching.
# google/vim-searchindex doesn't work better than henrik/vim-indexed-search.
flavor 'henrik/vim-indexed-search', branch: 'master'

# == Snippets

# The ultimate snippet solution for Vim.
flavor 'SirVer/ultisnips', branch: 'master'

# vim-snipmate default snippets
flavor 'honza/vim-snippets', branch: 'master'

# == Tags

# Vim plugin that displays tags in a window, ordered by scope
flavor 'majutsushi/tagbar', branch: 'master'

# Ctags generator for Vim
flavor 'szw/vim-tags', branch: 'master'

# == UI layout

# lean & mean status/tabline for vim that's light as air
flavor 'vim-airline/vim-airline', branch: 'master'

# precision colorscheme for the vim text editor 
flavor 'altercation/vim-colors-solarized', branch: 'master'

# A 24bit colorscheme for Vim, Airline and Lightline
flavor 'jacoborus/tender.vim', branch: 'master'

# Configure tabs within Terminal Vim
flavor 'mkitt/tabline.vim', branch: 'master'

# == Utils

# enable repeating supported plugin maps with "."
flavor 'tpope/vim-repeat', branch: 'master'

# Granular project configuration
flavor 'UzxMx/vim-projectionist', branch: 'master'

# quoting/parenthesizing made simple
flavor 'UzxMx/vim-surround', branch: 'master'

# Vim motions on speed!
flavor 'easymotion/vim-easymotion', branch: 'master'

# Send command from vim to a running tmux session
flavor 'jgdavey/tslime.vim', branch: 'master'

# Seamless navigation between tmux panes and vim splits
flavor 'christoomey/vim-tmux-navigator', branch: 'master'

# Pipe a visual selection to Mac OS X's pbcopy utility
flavor 'ahw/vim-pbcopy', branch: 'master'

# tmux basics
flavor 'tpope/vim-tbone', branch: 'master'

# Helpers for UNIX
flavor 'tpope/vim-eunuch'

# Defaults everyone can agree on
flavor 'tpope/vim-sensible'

# Pairs of handy bracket mappings
flavor 'tpope/vim-unimpaired', branch: 'master'

# Readline style insertion
flavor 'tpope/vim-rsi', branch: 'master'

# Substitution made easy
flavor 'tpope/vim-abolish', branch: 'master'

# emmet-vim is a vim plug-in which provides support for expanding abbreviations similar to emmet.
flavor 'mattn/emmet-vim', branch: 'master'

flavor 'editorconfig/editorconfig-vim', branch: 'master'

# == Navigation

# Plugin to toggle, display and navigate marks
flavor 'kshenoy/vim-signature', branch: 'master'

# == Plugin development

# A Vim plugin for Vim plugins
flavor 'tpope/vim-scriptease', branch: 'master'

# Vim plugin: Define your own operator easily
flavor 'kana/vim-operator-user'
# flavor 'haya14busa/vim-operator-flashy'

# Hex editing in Vim
# Also see http://vim.wikia.com/wiki/Hex_dump
flavor 'fidian/hexmode', branch: 'master'
